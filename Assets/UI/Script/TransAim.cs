using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using MyLibrary;

public class TransAim : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //MyRay.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
